<!DOCTYPE html>
<html>
  <head>
    <title>Shirumen</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if user_signed_in? %>
    <div class="post-card">
        <% @posts.each do |post| %>
          <div class="card">
            <div class="stream-item">
              <div class="content">
                <div class="stream-item-header">
                  <%= image_tag current_user.profile_photo, class: "avatar avatar-size48" %>
                  <div class="user-profile-link">
                    <%= link_to "@" + "#{post.user.username}", "#", class: "username" %>
                  </div>
                </div>
                <div class="post-text">
                  <%= safe_join(post.text.split("\n"), tag(:br)) %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
        <% else %>
          <div class="about-shirumen">
          <h1>汁麺とは</h1>
            <p class="shirumen-details">
              汁麺とはラーメン好きのためのSNSです。<br>
              麺を記し、麺を知る。<br>
              自分と同じような味覚の人や好みのラーメンを発見しましょう。<br>
              素敵なラーメンライフを。
            </p>
          </div>
        <% end %>
    </div>
  </body>
</html>